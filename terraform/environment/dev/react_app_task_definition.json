[
    {
        "name": "${container_name}",
        "image": "${image_url}",
        "cpu": 128,
        "memory": 128,
        "essential": true,
        "portMappings": [
            {
                "hostPort": 5000,
                "protocol": "tcp",
                "containerPort": 5000
            }
        ],
        "command": [
            "npm",
            "start"
        ]
    }
]
